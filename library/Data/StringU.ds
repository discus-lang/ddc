{-# OPTIONS -no-implicit-prelude #-}

module	Data.StringU
import	Base
	Foreign.Ptr
	DDC.Runtime.Store


-- | Get a single character from a string.
unsafeIndexStringU :: Ptr# (String# %r1) -> Int# -> Word8#
unsafeIndexStringU str pos
 = do	ptr8	= coercePtr str
	peekOnPtr str (plusPtr ptr8 pos)

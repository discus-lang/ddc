
import Base.Types;
import System.Console;

import extern
{
	primError 
		:: forall %r1 a
		.  String %r1	-> a
		:$ Obj -> Obj;

	primExit
		:: forall %r1 a
		.  Int %r1	-> a
		:$ Obj -> Obj;
}


error	= primError;

-- Imports

-- Pragmas

-- Infix

-- Data
data Fruit %rDK15
        = Apple
        
        | Orange;

data Foo %rDK16 a
        = FooInt {
                Base.Int %rDK16;
        }
        
        | FooFloat {
                Base.Float %rDK16;
        }
        
        | FooFun {
                a -> a;
        };

data Bar %rDK17 a
        = BarFoo {
                Main.Foo %rDK17 a;
        };


-- Effects

-- Regions

-- Classes

-- Class dictionaries

-- Class instances

-- Foreign imports

-- Binds
foreign import fi
        :: forall t0
        .  Foo %rTS0 t0
        :$ Base.Data;
        
foreign import ff
        :: forall t0
        .  Foo %rTS1 t0
        :$ Base.Data;
        
foreign import ffun
        :: Foo %rTS2 (Base.Int %rTS6)
        :- Base.Const %rTS6
        :$ Base.Data;
        
foreign import succ
        :: forall %r0 %r1
        .  Base.Int %r0 -(!e0)> Base.Int %r1
        :- !e0        = Base.!Read %r0
        :$ Base.Data -> Base.Data;
        
foreign import main
        :: Base.Unit -> Base.Unit
        :$ Base.Data -> Base.Data;
        



module Main
import Data.Maybe
import Data.Function
import System.IO.Console
where


derp (x: Nat#): Nat#
 | x == 0               = 10

{-
 = case x of
        Just x'         
         | x' == 0      -> 10

        Nothing         -> 20
-}

main (_: Unit): S Console Unit
 = do
        writel $ showNat (derp 0)
        writel $ showNat (derp 1)


--        writel $ showNat (derp (Just 0))
--        writel $ showNat (derp Nothing)
--        writel $ showNat (derp (Just 1))



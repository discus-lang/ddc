:set lang Tetra

-- Simple lambda abstraction
:check \(x : Unit). x


-- Lambda with multiple binders of the same type.
:check /\(r1 r2 r3 : Region). \(x: Int#). x


-- Lambda with multiple binders after the same lambda token.
:check /\(r1 : Region) (r2 : Region). \(x : Int#). x


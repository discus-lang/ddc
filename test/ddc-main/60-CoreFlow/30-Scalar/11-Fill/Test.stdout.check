ok
ok

-- | Single map over input series, writing into existing vector.
module Test with
letrec {
  test : [t0 : Rate].Vector# Int# -> Series# t0 Int# -> Unit
    = /\(t0 : Rate).
       \(x1 : Vector# Int#).\(x2 : Series# t0 Int#).
      let _
            = loop# [t0]
                  (\(x0 : Nat#).
                   let x2$elem = next# [Int#] [t0] x2 x0 in
                   let x3 = mul# [Int#] x2$elem 2i# in
                   let x7$elem = add# [Int#] x3 1i# in
                   let _ = vwrite# [Int#] x1 x0 x7$elem in
                   ()) in
      ()
}

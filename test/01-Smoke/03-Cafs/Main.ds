{-# OPTIONS -no-implicit-prelude #-}

import Base
import Data.Int

foreign import "printf"
 printf :: String#{read} -> Int#

one	= 1
zero	= 0

countDown n
 = if n == zero
	then printf "Done!\n"#
	else countDown (n - one)

main () 
 = do	countDown 100
	()

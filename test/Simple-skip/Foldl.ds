
import Base;

data	List %r1 a
	= Nil
	| Cons { 
		x 	:: a;
		xs	:: List %r1 a; };


foldl :: (s -> a -> s) -> s -> [a] -> s;
foldl    f state xx
 = case xx of {
 	[]	-> state;
	x : xs	
	-> do {	x'	= f state x;
		foldl f x' xs; };
 };


module Test with
letrec {
  fac : Nat# -> Nat# -> Nat#
    = \(acc n : Nat#).
      case n of {
        0#  
         -> acc;
        1#  
         -> acc;
        _ 
         -> fac (add# [Nat#] acc n) (sub# [Nat#] n 1#)
      }
}

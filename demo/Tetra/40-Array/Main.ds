
module Main
import System.IO.Console
import Data.Numeric.Nat
import Data.Function
import Data.Array
where


main ()
 = private r with { Alloc r; Read r; Write r } in
   do   arr     = allocArray [r] 10 0
        writeArray arr 5 100
        writel $ showNat (readArray arr 5)

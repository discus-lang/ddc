
module Main 
import System.IO.Console
where

main (_ : Unit) : S Console Unit
 = box do
        run writel (TextLit "hello world")
        run writel (TextLit "Καλημέρα κόσμε")
        run writel (TextLit "こんにちは 世界")
        run writel (TextLit "হ্যালো বিশ্ব")
